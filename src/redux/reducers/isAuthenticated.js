const isAuthenticated = (state=false, action) => {
    return state
    switch (action.type) {
        case 'authenticated':
            
            break;
    
        default:
            break;
    }
}

export default isAuthenticated